<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.App.Base" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="colorControlNormal">@color/material_primary</item>
        <item name="colorControlActivated">@color/material_primary</item>
        <item name="colorControlHighlight">#ff00ff</item>
        <item name="android:textColorHint">@color/material_primary</item>
        <item name="android:textColor">@color/material_primary</item>

    </style>

</resources>
